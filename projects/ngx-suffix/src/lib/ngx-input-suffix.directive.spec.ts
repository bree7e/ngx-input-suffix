import { NgxInputSuffixDirective } from './ngx-input-suffix.directive';

describe('NgxInputSuffixDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxInputSuffixDirective();
    expect(directive).toBeTruthy();
  });
});
